import VehicleType from '@/components/Page/VehicleType/VehicleType';
import { NextPage } from 'next';

const Page: NextPage = () => {
    return (
        <div>
            <VehicleType />
        </div>
    );
};

export default Page;